- players.each do |player|
  %tr
    %td= link_to player.name, player, "data-turbo" => "false"
    %td.text-center= player.sca_rating
    %td.text-center= player.fide_rating
    %td.text-center= player.federation
    %td.d-none.d-md-table-cell= player_roles(player.roles).truncate(50)
