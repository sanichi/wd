:ruby
  inputs = [
    [:handle,     t("user.handle"),      :text],
    [:password,   t("user.password"),    :password],
    [:roles,      t("user.roles.roles"), :select, menu: user_role_menu(user.roles), multiple: true, size: User::ALLOWED_ROLES.size],
    [:first_name, t("user.first_name"),  :text],
    [:last_name,  t("user.last_name"),   :text],
  ]
= render "utils/crud/form", object: user, index: users_path, inputs: inputs, klass: center(lg: 4, md: 6, sm: 8, xs: 10)
